# 📊 سیستم امتیازدهی (Scoring System)

## 🎯 بازه امتیازها

### محدوده داخلی (Internal - برای ML و محاسبات):

در تمام بخش‌های داخلی سیستم، امتیازها در بازه **[-1, +1]** هستند:

```
-1.0 ◄─────────────── 0 ──────────────► +1.0
نزولی قوی           خنثی           صعودی قوی
```

### محدوده نمایشی (Display - برای API و کاربر):

در خروجی API برای میکروسرویس، امتیازها در بازه **[-100, +100]** نمایش داده می‌شوند:

```
-100 ◄─────────────── 0 ──────────────► +100
نزولی قوی           خنثی           صعودی قوی
```

**⚠️ نکته مهم**: 
- **داخلی**: همه محاسبات ML با [-1, +1] انجام می‌شود
- **خروجی API**: فقط در نمایش به [-100, +100] تبدیل می‌شود
- **تبدیل**: `display_score = internal_score × 100`

### معنای اعداد (محدوده داخلی):

```python
امتیاز = +1.0  →  بسیار صعودی (VERY BULLISH)         → API: +100
امتیاز = +0.75 →  صعودی قوی (STRONG BULLISH)        → API: +75
امتیاز = +0.5  →  صعودی (BULLISH)                   → API: +50
امتیاز = +0.25 →  صعودی ضعیف (WEAK BULLISH)         → API: +25
امتیاز = 0.0   →  خنثی (NEUTRAL)                    → API: 0
امتیاز = -0.25 →  نزولی ضعیف (WEAK BEARISH)         → API: -25
امتیاز = -0.5  →  نزولی (BEARISH)                   → API: -50
امتیاز = -0.75 →  نزولی قوی (STRONG BEARISH)        → API: -75
امتیاز = -1.0  →  بسیار نزولی (VERY BEARISH)        → API: -100
```

### معنای اعتماد (Confidence):

**محدوده داخلی**: [0, 1]
**محدوده نمایشی**: [0, 100]

```python
اعتماد = 1.0  →  عالی (EXCELLENT)              → API: 100%
اعتماد = 0.9  →  خوب (HIGH)                   → API: 90%
اعتماد = 0.8  →  متوسط به بالا (GOOD)          → API: 80%
اعتماد = 0.7  →  متوسط (MEDIUM)               → API: 70%
اعتماد = 0.6  →  ضعیف (LOW)                   → API: 60%
اعتماد = 0.5  →  بسیار ضعیف (VERY LOW)        → API: 50%
```

---

## 🌐 فرمت خروجی API

### استفاده در میکروسرویس:

```python
from api.response_formatters import format_combined_response

# تحلیل (امتیازها داخلی: [-1, +1])
trend_result = trend_analyzer.analyze(trend_features)
momentum_result = momentum_analyzer.analyze(momentum_features)

# فرمت برای API (تبدیل به [-100, +100])
api_response = format_combined_response(
    trend_result,
    momentum_result,
    trend_weight=0.6,
    momentum_weight=0.4,
    use_persian=False
)

return jsonify(api_response)
```

### مثال خروجی JSON:

```json
{
  "analysis_type": "COMBINED",
  "trend": {
    "horizons": {
      "3d": {"score": 85, "confidence": 82, "signal": "VERY_BULLISH"},
      "7d": {"score": 75, "confidence": 78, "signal": "BULLISH"},
      "30d": {"score": 60, "confidence": 75, "signal": "BULLISH"}
    },
    "overall": {
      "score": 73,
      "confidence": 78,
      "signal": "BULLISH",
      "recommendation": "BUY"
    }
  },
  "momentum": {
    "horizons": {
      "3d": {"score": -20, "confidence": 70, "signal": "WEAK_BEARISH"},
      "7d": {"score": 30, "confidence": 72, "signal": "WEAK_BULLISH"},
      "30d": {"score": 55, "confidence": 68, "signal": "BULLISH"}
    },
    "overall": {
      "score": 22,
      "confidence": 70,
      "signal": "WEAK_BULLISH"
    }
  },
  "combined": {
    "score": 50,
    "confidence": 74,
    "signal": "BULLISH",
    "action": "ACCUMULATE"
  }
}
```

**توجه**: همه `score` ها در بازه [-100, +100] و `confidence` در [0, 100] هستند.

---

## 1️⃣ امتیازدهی در سطح اندیکاتور (Indicator Level)

### الف) اندیکاتورهای روند (10 اندیکاتور)

#### 1. SMA / EMA / WMA / DEMA / TEMA
```python
محاسبه اختلاف قیمت از میانگین متحرک:

diff = (قیمت فعلی - MA) / MA × 100

امتیازدهی (داخلی):
  diff > +5%   → امتیاز = +1.0 (بسیار صعودی)  [API: +100]
  diff > +2%   → امتیاز = +0.7 (صعودی)        [API: +70]
  diff > +0.5% → امتیاز = +0.3 (صعودی شکسته)  [API: +30]
  |diff| ≤ 0.5% → امتیاز = 0.0 (خنثی)        [API: 0]
  diff < -0.5% → امتیاز = -0.3 (نزولی شکسته) [API: -30]
  diff < -2%   → امتیاز = -0.7 (نزولی)       [API: -70]
  diff < -5%   → امتیاز = -1.0 (بسیار نزولی) [API: -100]

مثال:
  قیمت = $50,000
  SMA(20) = $48,000
  diff = (50000 - 48000) / 48000 × 100 = 4.17%
  
  → امتیاز SMA = +0.7 (صعودی)
```

#### 2. MACD
```python
MACD Line = EMA(12) - EMA(26)
Signal Line = EMA(MACD, 9)
Histogram = MACD - Signal

امتیازدهی بر اساس Histogram:
  
  if Histogram > 0:
      if Histogram افزایش یافته:
          امتیاز = +1.0 (بسیار صعودی)
      else:
          امتیاز = +0.5 (صعودی شکسته شده)
  
  elif Histogram < 0:
      if Histogram کاهش یافته:
          امتیاز = -1.0 (بسیار نزولی)
      else:
          امتیاز = -0.5 (نزولی شکسته شده)
  
  else:
      امتیاز = 0.0 (خنثی)

مثال:
  MACD = 120
  Signal = 90
  Histogram = +30 (در حال افزایش)
  
  → امتیاز MACD = +1.0
```

#### 3. ADX
```python
ADX = میانگین Directional Index
+DI = Positive Directional Indicator
-DI = Negative Directional Indicator

امتیازدهی:
  if +DI > -DI:  # صعودی
      if ADX > 40:
          امتیاز = +1.0 (روند صعودی بسیار قوی)
      elif ADX > 25:
          امتیاز = +0.7 (روند صعودی قوی)
      elif ADX > 20:
          امتیاز = +0.5 (روند صعودی متوسط)
      else:
          امتیاز = +0.2 (روند ضعیف)
  
  elif -DI > +DI:  # نزولی
      if ADX > 40:
          امتیاز = -1.0
      elif ADX > 25:
          امتیاز = -0.7
      elif ADX > 20:
          امتیاز = -0.5
      else:
          امتیاز = -0.2
  
  else:
      امتیاز = 0.0

مثال:
  ADX = 35
  +DI = 30
  -DI = 15
  
  → +DI > -DI (صعودی)
  → ADX = 35 > 25
  → امتیاز ADX = +0.7
```

#### 4. Parabolic SAR
```python
امتیازدهی:
  if قیمت > SAR:
      امتیاز = +1.0 (صعودی)
  else:
      امتیاز = -1.0 (نزولی)

مثال:
  قیمت = $50,000
  SAR = $49,000
  
  → امتیاز SAR = +1.0
```

#### 5. Supertrend
```python
امتیازدهی:
  if قیمت > Supertrend:
      امتیاز = +1.0 (صعودی)
  else:
      امتیاز = -1.0 (نزولی)

مثال:
  قیمت = $50,000
  Supertrend = $48,500
  
  → امتیاز Supertrend = +1.0
```

#### 6. Ichimoku Cloud
```python
امتیازدهی:
  if قیمت > Senkou Span A و قیمت > Senkou Span B:
      امتیاز = +1.0 (بالای ابر - صعودی)
  elif قیمت < Senkou Span A و قیمت < Senkou Span B:
      امتیاز = -1.0 (زیر ابر - نزولی)
  else:
      امتیاز = 0.0 (داخل ابر - خنثی)

مثال:
  قیمت = $50,000
  Senkou Span A = $48,000
  Senkou Span B = $47,500
  
  → امتیاز Ichimoku = +1.0
```

---

### ب) اندیکاتورهای مومنتوم (8 اندیکاتور)

#### 1. RSI (Relative Strength Index)
```python
RSI در بازه [0, 100]

امتیازدهی:
  if RSI > 70:
      امتیاز = -0.5 (Overbought - احتمال اصلاح)
  elif RSI > 60:
      امتیاز = +0.7 (صعودی قوی)
  elif RSI > 50:
      امتیاز = +0.4 (صعودی)
  elif RSI > 40:
      امتیاز = -0.4 (نزولی)
  elif RSI > 30:
      امتیاز = -0.7 (نزولی قوی)
  else:  # RSI < 30
      امتیاز = +0.5 (Oversold - احتمال برگشت)

مثال:
  RSI = 65
  
  → امتیاز RSI = +0.7 (صعودی قوی)
```

#### 2. Stochastic Oscillator
```python
Stochastic %K و %D در بازه [0, 100]

امتیازدهی:
  if %K > 80:
      امتیاز = -0.5 (Overbought)
  elif %K > 50:
      امتیاز = +0.7 (صعودی)
  elif %K > 20:
      امتیاز = -0.7 (نزولی)
  else:
      امتیاز = +0.5 (Oversold)

مثال:
  %K = 70
  
  → امتیاز Stochastic = +0.7
```

#### 3. CCI (Commodity Channel Index)
```python
CCI معمولاً در بازه [-200, +200]

امتیازدهی:
  if CCI > +100:
      امتیاز = +1.0 (صعودی قوی)
  elif CCI > 0:
      امتیاز = +0.5 (صعودی)
  elif CCI > -100:
      امتیاز = -0.5 (نزولی)
  else:
      امتیاز = -1.0 (نزولی قوی)

مثال:
  CCI = 150
  
  → امتیاز CCI = +1.0
```

#### 4. Williams %R
```python
Williams %R در بازه [-100, 0]

امتیازدهی:
  if %R > -20:
      امتیاز = -0.5 (Overbought)
  elif %R > -50:
      امتیاز = +0.7 (صعودی)
  elif %R > -80:
      امتیاز = -0.7 (نزولی)
  else:
      امتیاز = +0.5 (Oversold)

مثال:
  %R = -35
  
  → امتیاز Williams %R = +0.7
```

#### 5-8. ROC, Momentum, OBV, CMF
```python
هر کدام امتیازی در بازه [-1, +1] بر اساس فرمول خود
```

---

## 2️⃣ امتیازدهی در سطح دسته (Category Level)

### الف) امتیاز کل روند (Trend Score)

```python
محاسبه از 10 اندیکاتور روند:

Trend Score = Σ(امتیاز اندیکاتور × اعتماد اندیکاتور) / Σ(اعتماد اندیکاتورها)

مثال:
  SMA:         +0.7 × 0.80 = 0.560
  EMA:         +0.7 × 0.85 = 0.595
  MACD:        +1.0 × 0.88 = 0.880
  ADX:         +0.7 × 0.90 = 0.630
  SAR:         +1.0 × 0.75 = 0.750
  Supertrend:  +1.0 × 0.80 = 0.800
  Ichimoku:    +1.0 × 0.85 = 0.850
  WMA:         +0.7 × 0.70 = 0.490
  DEMA:        +0.7 × 0.75 = 0.525
  TEMA:        +0.7 × 0.78 = 0.546
  
  مجموع = 6.626
  مجموع اعتماد = 7.96
  
  Trend Score = 6.626 / 7.96 = 0.832
  
  → امتیاز کل روند = +0.83 (صعودی قوی)
```

### ب) امتیاز کل مومنتوم (Momentum Score)

```python
محاسبه از 8 اندیکاتور مومنتوم:

Momentum Score = Σ(امتیاز اندیکاتور × اعتماد) / Σ(اعتماد)

مثال:
  RSI:        +0.7 × 0.75 = 0.525
  Stochastic: +0.7 × 0.70 = 0.490
  CCI:        +1.0 × 0.80 = 0.800
  Williams:   +0.7 × 0.70 = 0.490
  ROC:        +0.5 × 0.65 = 0.325
  Momentum:   +0.5 × 0.65 = 0.325
  OBV:        +0.8 × 0.75 = 0.600
  CMF:        +0.6 × 0.70 = 0.420
  
  مجموع = 3.975
  مجموع اعتماد = 5.70
  
  Momentum Score = 3.975 / 5.70 = 0.697
  
  → امتیاز کل مومنتوم = +0.70 (صعودی)
```

---

## 3️⃣ امتیازدهی Multi-Horizon (چند افقی)

### هر افق زمانی امتیاز مستقل دارد:

```python
3d Score:  امتیاز برای 3 روز آینده  [-1, +1]
7d Score:  امتیاز برای 7 روز آینده  [-1, +1]
30d Score: امتیاز برای 30 روز آینده [-1, +1]

مثال:
  روند:
    3d:  +0.85 (صعودی قوی کوتاه‌مدت)
    7d:  +0.75 (صعودی میان‌مدت)
    30d: +0.60 (صعودی بلندمدت)
  
  مومنتوم:
    3d:  -0.20 (ضعیف نزولی - اصلاح کوتاه‌مدت)
    7d:  +0.30 (صعودی ضعیف)
    30d: +0.55 (صعودی)
```

### ترکیب Multi-Horizon:

```python
Combined Score = (Trend × وزن_روند) + (Momentum × وزن_مومنتوم)

مثال با وزن‌های 50-50:
  3d Combined = (0.85 × 0.5) + (-0.20 × 0.5)
              = 0.425 + (-0.10)
              = +0.325 (صعودی ضعیف)
  
  7d Combined = (0.75 × 0.5) + (0.30 × 0.5)
              = 0.375 + 0.15
              = +0.525 (صعودی)
  
  30d Combined = (0.60 × 0.5) + (0.55 × 0.5)
               = 0.30 + 0.275
               = +0.575 (صعودی)
```

---

## 4️⃣ سطح اعتماد (Confidence Level)

اعتماد همیشه در بازه **[0, 1]** است:

```
0.0 ◄────────────────────────► 1.0
هیچ اعتماد               اعتماد کامل
```

### معنای اعداد اعتماد:

```python
confidence = 0.9-1.0  →  اعتماد بسیار بالا (Excellent)
confidence = 0.8-0.9  →  اعتماد بالا (High)
confidence = 0.7-0.8  →  اعتماد خوب (Good)
confidence = 0.6-0.7  →  اعتماد متوسط (Medium)
confidence = 0.5-0.6  →  اعتماد ضعیف (Low)
confidence < 0.5      →  اعتماد بسیار ضعیف (Very Low)
```

### محاسبه اعتماد در سطح اندیکاتور:

```python
# برای میانگین‌های متحرک:
slope = (MA امروز - MA دیروز) / قیمت
confidence = 0.6 + min(|slope| × 100, 0.35)
confidence = min(confidence, 0.95)

مثال:
  SMA امروز = $48,500
  SMA دیروز = $48,000
  قیمت = $50,000
  
  slope = (48500 - 48000) / 50000 = 0.01
  confidence = 0.6 + min(0.01 × 100, 0.35)
             = 0.6 + 0.35
             = 0.95 ✅
  
  → اعتماد SMA = 0.95 (بسیار بالا)
```

### محاسبه اعتماد در سطح دسته:

```python
Category Confidence = Σ(اعتماد اندیکاتورها) / تعداد اندیکاتورها

مثال:
  10 اندیکاتور روند با اعتماد:
  [0.80, 0.85, 0.88, 0.90, 0.75, 0.80, 0.85, 0.70, 0.75, 0.78]
  
  Trend Confidence = (0.80+0.85+0.88+0.90+0.75+0.80+0.85+0.70+0.75+0.78) / 10
                   = 7.96 / 10
                   = 0.796
  
  → اعتماد روند = 0.80 (بالا)
```

---

## 5️⃣ جدول خلاصه امتیازدهی

### الف) امتیازها (Score)

| بازه امتیاز | معنی | رنگ | توصیه |
|--------------|------|-----|-------|
| +0.8 تا +1.0 | بسیار صعودی | 🟢🟢 | STRONG BUY |
| +0.5 تا +0.8 | صعودی قوی | 🟢 | BUY |
| +0.2 تا +0.5 | صعودی ضعیف | 🟡 | ACCUMULATE |
| -0.2 تا +0.2 | خنثی | ⚪ | HOLD |
| -0.5 تا -0.2 | نزولی ضعیف | 🟠 | TAKE PROFIT |
| -0.8 تا -0.5 | نزولی قوی | 🔴 | SELL |
| -1.0 تا -0.8 | بسیار نزولی | 🔴🔴 | STRONG SELL |

### ب) اعتماد (Confidence)

| بازه اعتماد | کیفیت | معنی |
|-------------|--------|------|
| 0.9 - 1.0 | عالی | همه اندیکاتورها هم‌راستا |
| 0.8 - 0.9 | بالا | اکثریت قاطع اندیکاتورها |
| 0.7 - 0.8 | خوب | اکثریت اندیکاتورها |
| 0.6 - 0.7 | متوسط | توافق نسبی |
| 0.5 - 0.6 | ضعیف | اختلاف نظر زیاد |
| < 0.5 | بسیار ضعیف | سیگنال‌های متناقض |

---

## 6️⃣ مثال کامل عملی

### وضعیت بازار Bitcoin:

```python
قیمت فعلی: $50,000

# ════════════════════════════════════════
# اندیکاتورهای روند (امتیاز × اعتماد)
# ════════════════════════════════════════
SMA(20):      +0.70 × 0.80 = 0.560
EMA(20):      +0.70 × 0.85 = 0.595
WMA(20):      +0.70 × 0.70 = 0.490
DEMA(20):     +0.70 × 0.75 = 0.525
TEMA(20):     +0.70 × 0.78 = 0.546
MACD:         +1.00 × 0.88 = 0.880
ADX:          +0.70 × 0.90 = 0.630
SAR:          +1.00 × 0.75 = 0.750
Supertrend:   +1.00 × 0.80 = 0.800
Ichimoku:     +1.00 × 0.85 = 0.850

Trend Score = 6.626 / 7.96 = 0.832 ✅
Trend Confidence = 7.96 / 10 = 0.796 ✅

# ════════════════════════════════════════
# اندیکاتورهای مومنتوم (امتیاز × اعتماد)
# ════════════════════════════════════════
RSI:          +0.70 × 0.75 = 0.525
Stochastic:   +0.70 × 0.70 = 0.490
CCI:          +1.00 × 0.80 = 0.800
Williams %R:  +0.70 × 0.70 = 0.490
ROC:          +0.50 × 0.65 = 0.325
Momentum:     +0.50 × 0.65 = 0.325
OBV:          +0.80 × 0.75 = 0.600
CMF:          +0.60 × 0.70 = 0.420

Momentum Score = 3.975 / 5.70 = 0.697 ✅
Momentum Confidence = 5.70 / 8 = 0.713 ✅

# ════════════════════════════════════════
# Multi-Horizon Scores
# ════════════════════════════════════════
Trend 3d:     +0.85 (85% اعتماد)
Trend 7d:     +0.75 (82% اعتماد)
Trend 30d:    +0.60 (78% اعتماد)

Momentum 3d:  -0.20 (70% اعتماد)
Momentum 7d:  +0.30 (68% اعتماد)
Momentum 30d: +0.55 (72% اعتماد)

# ════════════════════════════════════════
# Combined Scores (وزن 50-50)
# ════════════════════════════════════════
Combined 3d:  +0.325 (78% اعتماد) → ACCUMULATE
Combined 7d:  +0.525 (75% اعتماد) → BUY
Combined 30d: +0.575 (75% اعتماد) → BUY

# ════════════════════════════════════════
# تفسیر نهایی
# ════════════════════════════════════════
✅ روند: صعودی قوی (+0.83)
⚠️ مومنتوم: صعودی (+0.70) اما در کوتاه‌مدت ضعیف
💡 توصیه: روند بلندمدت قوی است، مومنتوم کوتاه‌مدت ضعیف
   
   → Day Trader: صبر برای اصلاح
   → Swing Trader: نگهداری یا خرید در اصلاح
   → Position Trader: نگهداری قوی
```

---

## 7️⃣ فرمول‌های کلیدی

### امتیاز نهایی:
```python
Final Score = Σ(Score_i × Confidence_i × Weight_i) / Σ(Confidence_i × Weight_i)

که در آن:
  Score_i: امتیاز دسته i [-1, +1]
  Confidence_i: اعتماد دسته i [0, 1]
  Weight_i: وزن دسته i (مثلاً 0.5 برای روند)
```

### اعتماد نهایی:
```python
Final Confidence = (Agreement × 0.6) + (Average_Confidence × 0.4)

که در آن:
  Agreement: توافق بین دسته‌ها [0, 1]
  Average_Confidence: میانگین اعتماد دسته‌ها
```

---

## 📋 خلاصه

✅ **امتیازها**: همیشه در بازه `[-1, +1]`
- +1: بسیار صعودی
- 0: خنثی
- -1: بسیار نزولی

✅ **اعتماد**: همیشه در بازه `[0, 1]`
- 1.0: اعتماد کامل
- 0.5: اعتماد متوسط
- 0: هیچ اعتماد

✅ **سه سطح**:
1. سطح اندیکاتور (هر اندیکاتور یک امتیاز)
2. سطح دسته (Trend, Momentum)
3. سطح Multi-Horizon (3d, 7d, 30d)

✅ **وزن‌دهی**:
- امتیازهای با اعتماد بالاتر تأثیر بیشتری دارند
- ترکیب با میانگین وزنی
