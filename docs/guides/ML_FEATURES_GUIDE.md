# راهنمای ویژگی‌ها و هدف در یادگیری ماشین

## 📊 مفاهیم پایه

در یادگیری ماشین، ما به مدل می‌آموزیم که از **ویژگی‌ها (Features)** برای پیش‌بینی **هدف (Target/Label)** استفاده کند.

```
Features (X) → Model → Target (y)
```

### در سیستم ما:

**هدف یادگیری:** پیش‌بینی جهت و قدرت حرکت قیمت در آینده

```python
# ویژگی‌ها (X): اطلاعات فعلی بازار
[سیگنال SMA، دقت SMA، سیگنال EMA، ...]

# هدف (y): بازده آینده
return = (قیمت 3 روز بعد - قیمت فعلی) / قیمت فعلی
```

---

## 🎯 سطح 1: یادگیری وزن اندیکاتورها

### ❓ سوال: کدام اندیکاتور مهم‌تر است؟

**مشکل سیستم قبلی:**
```python
# وزن‌های ثابت (دستی)
sma_weight = 0.15
ema_weight = 0.15
macd_weight = 0.20
adx_weight = 0.20
# ...
```

❌ این وزن‌ها بدون پشتوانه علمی انتخاب شده‌اند!

### ✅ راه‌حل: یادگیری ماشین

**ویژگی‌ها (21 ویژگی از 7 اندیکاتور):**

```python
{
    # اندیکاتور SMA
    'sma_signal': 0.8,        # سیگنال نرمال شده [-1, 1]
    'sma_confidence': 0.75,   # دقت SMA
    'sma_weighted': 0.6,      # signal × confidence
    
    # اندیکاتور EMA
    'ema_signal': 1.0,        # صعودی قوی
    'ema_confidence': 0.85,   # دقت بالا
    'ema_weighted': 0.85,     # 1.0 × 0.85
    
    # اندیکاتور WMA
    'wma_signal': 0.6,
    'wma_confidence': 0.7,
    'wma_weighted': 0.42,
    
    # اندیکاتور DEMA
    'dema_signal': 0.9,
    'dema_confidence': 0.75,
    'dema_weighted': 0.675,
    
    # اندیکاتور TEMA
    'tema_signal': 1.0,
    'tema_confidence': 0.78,
    'tema_weighted': 0.78,
    
    # اندیکاتور MACD
    'macd_signal': 0.7,
    'macd_confidence': 0.88,
    'macd_weighted': 0.616,
    
    # اندیکاتور ADX
    'adx_signal': 1.0,
    'adx_confidence': 0.9,
    'adx_weighted': 0.9
}
```

**هدف (Label):**

```python
# بازده 3 روز آینده
y = (price_day_3 - price_day_0) / price_day_0

# مثال:
قیمت امروز: $50,000
قیمت 3 روز بعد: $51,500

y = (51500 - 50000) / 50000 = 0.03 (3% سود)
```

### 📈 نحوه کار:

```python
# داده آموزش
X = [
    [0.8, 0.75, 0.6, 1.0, 0.85, 0.85, ...],  # روز 1 → بازده 3 روز بعد: +2%
    [0.6, 0.7, 0.42, 0.9, 0.8, 0.72, ...],   # روز 2 → بازده 3 روز بعد: -1%
    [1.0, 0.9, 0.9, 1.0, 0.85, 0.85, ...],   # روز 3 → بازده 3 روز بعد: +5%
    # ... 147 نمونه
]

y = [0.02, -0.01, 0.05, ...]

# آموزش مدل
model.fit(X, y)

# استخراج اهمیت ویژگی‌ها (Feature Importance)
importances = model.feature_importances_
  → sma_confidence: 83.0
  → adx_confidence: 43.0
  → ema_signal: 41.0
  → macd_signal: 19.0
  → ...
```

### 🔍 تبدیل به وزن:

```python
# گروه‌بندی بر اساس اندیکاتور
sma_importance = 83 + 5 + 14 = 102  # signal + confidence + weighted
ema_importance = 41 + 9 + 6 = 56
adx_importance = 43 + ... = 43
# ...

total = 102 + 56 + 43 + ... = 232

# وزن‌های یادگیری شده
sma_weight = 102 / 232 = 0.44 (44%)
ema_weight = 56 / 232 = 0.24 (24%)
adx_weight = 43 / 232 = 0.19 (19%)
```

**نتیجه:**

✅ **SMA مهم‌ترین اندیکاتور است** (44%)
✅ **ADX و EMA** نیز تأثیر قابل توجهی دارند
✅ **WMA** تأثیر کمی دارد (0%)

---

## 🎯 سطح 2: یادگیری وزن ابعاد

### ❓ سوال: کدام بُعد تحلیل مهم‌تر است؟

**4 بُعد تحلیل روند:**
1. اندیکاتورها (Indicators)
2. الگوهای شمعی (Candlestick)
3. امواج الیوت (Elliott Wave)
4. الگوهای کلاسیک (Classical Patterns)

**مشکل سیستم قبلی:**
```python
# وزن‌های ثابت پیشنهادی
indicators_weight = 0.40
elliott_weight = 0.30
classical_weight = 0.20
candlestick_weight = 0.10
```

❓ آیا واقعاً الیوت 30% تأثیر دارد؟

### ✅ راه‌حل: یادگیری ماشین

**ویژگی‌ها (12 ویژگی از 4 بُعد):**

```python
{
    # بُعد 1: اندیکاتورها
    'dim1_indicators_score': 0.89,       # امتیاز کلی اندیکاتورها
    'dim1_indicators_confidence': 0.86,  # دقت متوسط
    'dim1_indicators_weighted': 0.7654,  # 0.89 × 0.86
    
    # بُعد 2: الگوهای شمعی
    'dim2_candlestick_score': 0.5,       # خنثی
    'dim2_candlestick_confidence': 0.7,
    'dim2_candlestick_weighted': 0.35,
    
    # بُعد 3: امواج الیوت
    'dim3_elliott_score': 0.75,          # صعودی
    'dim3_elliott_confidence': 0.88,     # در موج 3
    'dim3_elliott_weighted': 0.66,
    
    # بُعد 4: الگوهای کلاسیک
    'dim4_classical_score': 0.8,         # شکست مثلث
    'dim4_classical_confidence': 0.75,
    'dim4_classical_weighted': 0.6
}
```

**هدف (Label):** همان بازده 3 روز آینده

```python
y = (price_day_3 - price_day_0) / price_day_0
```

### 📈 نحوه کار:

```python
# داده آموزش
X = [
    [0.89, 0.86, 0.77, 0.5, 0.7, 0.35, 0.75, 0.88, 0.66, 0.8, 0.75, 0.6],  # روز 1
    [0.6, 0.7, 0.42, 0.3, 0.6, 0.18, 0.5, 0.8, 0.4, 0.4, 0.7, 0.28],       # روز 2
    # ... 147 نمونه
]

y = [0.03, -0.01, ...]

# آموزش مدل
model.fit(X, y)

# استخراج اهمیت
importances = model.feature_importances_
  → dim1_indicators_confidence: 130.0
  → dim1_indicators_score: 65.0
  → dim4_classical_confidence: 28.0
  → dim3_elliott_score: 27.0
  → dim2_candlestick_*: 0.0  # بدون تأثیر!
```

### 🔍 تبدیل به وزن:

```python
indicators_importance = 130 + 65 + 10 = 205
elliott_importance = 27 + 13 + 0 = 40
classical_importance = 28 + 2 + 0 = 30
candlestick_importance = 0 + 0 + 0 = 0

total = 205 + 40 + 30 + 0 = 275

# وزن‌های یادگیری شده
indicators_weight = 205 / 275 = 0.745 (74.5%)
elliott_weight = 40 / 275 = 0.145 (14.5%)
classical_weight = 30 / 275 = 0.109 (10.9%)
candlestick_weight = 0 / 275 = 0.0 (0%)
```

**نتیجه:**

✅ **اندیکاتورها خیلی مهم‌تر از تصور بودند** (75% به جای 40%)
❌ **الیوت کمتر از تصور تأثیر دارد** (15% به جای 30%)
❌ **الگوهای شمعی تأثیر ندارند** (0% به جای 10%)

---

## 🧮 فرمول محاسبات

### سطح 1: امتیاز اندیکاتورها

**قبل از ML (وزن ثابت):**
```python
indicator_score = Σ(indicator_signal × fixed_weight)
  = (sma_signal × 0.15) + (ema_signal × 0.15) + ...
```

**بعد از ML (وزن یادگیری شده):**
```python
indicator_score = Σ(indicator_signal × learned_weight)
  = (sma_signal × 0.44) + (ema_signal × 0.24) + (adx_signal × 0.19) + ...
```

**با تعدیل اطمینان:**
```python
if R² < 0.4:  # مدل ضعیف
    reliability_factor = 0.4
    adjusted_weight = learned_weight × 0.4
    
    # ترکیب با وزن پیشنهادی
    final_weight = (learned_weight × 0.4) + (proposed_weight × 0.6)
```

### سطح 2: امتیاز نهایی

**قبل از ML:**
```python
overall_score = (indicators × 0.40) + (elliott × 0.30) + 
                (classical × 0.20) + (candlestick × 0.10)
```

**بعد از ML:**
```python
overall_score = (indicators × 0.745) + (elliott × 0.145) + 
                (classical × 0.109) + (candlestick × 0.0)
```

**با Blending (R² پایین):**
```python
if R² = -0.25:  # مدل ضعیف
    blend_ratio = 0.3 ML + 0.7 Proposed
    
    final_weight = (ml_weight × 0.3) + (proposed_weight × 0.7)
    
    indicators: (0.745 × 0.3) + (0.40 × 0.7) = 0.504 (50.4%)
    elliott: (0.145 × 0.3) + (0.30 × 0.7) = 0.254 (25.4%)
    classical: (0.109 × 0.3) + (0.20 × 0.7) = 0.173 (17.3%)
    candlestick: (0.0 × 0.3) + (0.10 × 0.7) = 0.07 (7%)
```

---

## 📊 مثال عملی کامل

### داده ورودی (یک نمونه آموزش):

```python
# کندل‌های 50 روز گذشته
candles = [Candle(45000, 45500, 44800, 45200, ...)] × 50

# محاسبه ویژگی‌ها
features = {
    'sma_signal': 0.8,
    'sma_confidence': 0.75,
    'sma_weighted': 0.6,
    'ema_signal': 1.0,
    'ema_confidence': 0.85,
    'ema_weighted': 0.85,
    # ... بقیه 15 ویژگی
}

# هدف (3 روز بعد)
candles_future = [Candle(45500, ...), Candle(46000, ...), Candle(47000, ...)]
target = (47000 - 45200) / 45200 = 0.0398 (3.98% سود)
```

### آموزش:

```python
# جمع‌آوری 147 نمونه مشابه
X_train = [
    [0.8, 0.75, 0.6, 1.0, 0.85, ...],  # نمونه 1
    [0.6, 0.7, 0.42, 0.9, 0.8, ...],   # نمونه 2
    # ...
]

y_train = [0.0398, -0.015, 0.062, ...]  # بازده واقعی

# آموزش LightGBM
model = LGBMRegressor()
model.fit(X_train, y_train)
```

### استخراج وزن:

```python
# اهمیت ویژگی‌ها
importances = model.feature_importances_
  [83, 43, 41, 19, 14, 9, 9, 6, 5, 3, ...]
  
# گروه‌بندی
weights = group_by_indicator(importances)
  → sma: 44%
  → ema: 24%
  → adx: 19%
  → macd: 8%
  → dema: 4%
  → tema: 1%
  → wma: 0%
```

### استفاده در پیش‌بینی:

```python
# داده جدید
new_candles = [50 کندل اخیر]

# محاسبه سیگنال با وزن‌های یادگیری شده
sma_result = TrendIndicators.sma(new_candles)
ema_result = TrendIndicators.ema(new_candles)
# ...

# امتیاز نهایی
trend_score = (sma_result.signal.score × 0.44) + 
              (ema_result.signal.score × 0.24) + 
              (adx_result.signal.score × 0.19) + 
              # ...

# نتیجه: 0.85 → صعودی قوی
```

---

## 🎓 چرا این روش بهتر است؟

### ❌ روش قبلی (وزن ثابت):

```python
# بر اساس حدس و تجربه
sma_weight = 0.15  # چرا 15%؟ کسی نمی‌داند!
```

**مشکلات:**
- بدون پشتوانه علمی
- در بازارهای مختلف متفاوت
- تغییر در طول زمان
- نمی‌دانیم دقت چقدر است

### ✅ روش جدید (ML):

```python
# بر اساس تحلیل 147 نمونه واقعی
sma_weight = 0.44  # چون در 147 نمونه، SMA بیشترین همبستگی را داشت
```

**مزایا:**
- ✅ علمی و قابل اثبات
- ✅ R² برای سنجش کیفیت
- ✅ Confidence Interval برای عدم قطعیت
- ✅ تطبیق خودکار با بازار
- ✅ قابلیت بازآموزی

---

## 📈 معیارهای ارزیابی

### 1. R² Score (ضریب تعیین)

```python
R² = 1 - (SS_residual / SS_total)

R² = 1.0  → پیش‌بینی کامل ✨
R² = 0.7  → خوب ✅
R² = 0.4  → متوسط ⚠️
R² = 0.0  → مدل بی‌فایده ❌
R² < 0    → مدل بدتر از میانگین ❌❌
```

**در تست ما:**
- Level 1: R² = -0.22 → ضعیف (دیتاست کوچک)
- Level 2: R² = -0.25 → ضعیف

**راه‌حل:** استفاده از 730 روز داده برای بهبود

### 2. MAE (میانگین خطای مطلق)

```python
MAE = Σ|y_true - y_pred| / n

MAE کم → دقت بالا
```

**در تست ما:**
- Level 1: MAE = 0.045 → خطای 4.5% در پیش‌بینی بازده
- Level 2: MAE = 0.046 → مشابه

### 3. Confidence Interval (فاصله اطمینان)

```python
CI_95% = ±1.96 × std(residuals)

پیش‌بینی: +3%
CI: ±10%
→ بازده واقعی بین -7% تا +13%
```

**در تست ما:**
- Level 1: CI = ±10.6%
- Level 2: CI = ±10.7%

---

## 🔄 چرخه کامل یادگیری

```
1. جمع‌آوری داده
   ↓
   [200 روز کندل Bitcoin]

2. استخراج ویژگی‌ها
   ↓
   [147 نمونه × 21 ویژگی]

3. محاسبه هدف
   ↓
   [بازده 3 روز آینده]

4. تقسیم داده
   ↓
   Train: 117 نمونه
   Test: 30 نمونه

5. آموزش مدل
   ↓
   LightGBM.fit(X_train, y_train)

6. ارزیابی
   ↓
   R² = -0.22 (ضعیف)
   MAE = 0.045

7. استخراج اهمیت
   ↓
   Feature Importance → Weights

8. تعدیل با Confidence
   ↓
   Reliability Factor = 0.4
   Adjusted Weights = Weights × 0.4

9. ذخیره مدل
   ↓
   ml_models/indicator_weights.json
   {
     weights: {...},
     metrics: {R²: -0.22, MAE: 0.045},
     adjusted_weights: {...}
   }

10. استفاده در تحلیل
    ↓
    trend_score = Σ(signal × adjusted_weight)
```

---

## 💡 نکات کلیدی

### 1. ویژگی‌ها (Features)

- **چیست؟** اطلاعاتی که به مدل می‌دهیم
- **در ما:** سیگنال‌ها و دقت‌های اندیکاتورها و ابعاد
- **تعداد:** 21 ویژگی (سطح 1) + 12 ویژگی (سطح 2)

### 2. هدف (Target)

- **چیست؟** چیزی که می‌خواهیم پیش‌بینی کنیم
- **در ما:** بازده قیمت 3 روز آینده
- **فرمول:** `(price_future - price_now) / price_now`

### 3. چرا 3 روز؟

- کوتاه‌مدت: واکنش سریع به تغییرات
- قابل پیش‌بینی: نه خیلی دور، نه خیلی نزدیک
- قابل تنظیم: می‌توان 1، 5، 7 روز هم امتحان کرد

### 4. چرا Regression (نه Classification)?

```python
# Regression: پیش‌بینی عدد دقیق
y = 0.03  # 3% سود

# Classification: پیش‌بینی کلاس
y = "UP"  # فقط جهت
```

**مزیت Regression:**
- اطلاعات بیشتر (مقدار دقیق)
- Feature Importance دقیق‌تر
- می‌توانیم وزن‌های پیوسته استخراج کنیم

### 5. چرا LightGBM؟

```python
# مزایا:
✅ سریع
✅ دقت بالا
✅ Feature Importance قوی
✅ مناسب داده‌های جدولی
✅ مقاوم در برابر overfitting
```

---

## 🔮 آینده: بهبودها

### 1. داده بیشتر

```python
# فعلی: 200 روز → 147 نمونه
# هدف: 730 روز (2 سال) → 677 نمونه
```

**انتظار:** R² بهتر، overfitting کمتر

### 2. ویژگی‌های بیشتر

```python
# فعلی: 21 ویژگی
# آینده: + حجم، + نوسان، + همبستگی بازارها
```

### 3. بازآموزی دوره‌ای

```python
# هر ماه: آموزش مجدد با داده‌های تازه
# تطبیق با تغییرات بازار
```

### 4. Multi-Target Learning

```python
# پیش‌بینی چند هدف همزمان
y1 = بازده 1 روز
y2 = بازده 3 روز
y3 = بازده 7 روز
```

---

## 📚 خلاصه

| مفهوم | توضیح | مثال |
|-------|-------|------|
| **Features** | ویژگی‌های ورودی | سیگنال SMA، دقت EMA، ... |
| **Target** | هدف پیش‌بینی | بازده 3 روز آینده |
| **Model** | الگوریتم یادگیری | LightGBM Regressor |
| **Training** | یادگیری از داده | 147 نمونه تاریخی |
| **Feature Importance** | اهمیت ویژگی‌ها | SMA: 83, ADX: 43, ... |
| **Weights** | وزن‌های یادگیری شده | SMA: 44%, EMA: 24%, ... |
| **R²** | کیفیت مدل | -0.22 (ضعیف، نیاز به داده بیشتر) |
| **Confidence Adjustment** | تعدیل با اطمینان | وزن × 0.4 (برای مدل ضعیف) |

**نتیجه نهایی:** سیستم وزن‌دهی علمی، قابل ارزیابی، و قابل بهبود! 🎯
