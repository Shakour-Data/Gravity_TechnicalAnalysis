# خلاصه تغییرات: لحاظ کردن دقت در امتیازدهی کلی

## 🎯 هدف
اضافه کردن **دقت (Accuracy/Confidence)** به فرآیند محاسبه امتیاز کلی تا تصمیم‌گیری هوشمندانه‌تر و دقیق‌تر شود.

## 📋 تغییرات انجام‌شده

### 1. ✅ ویرایش `models/schemas.py`

#### قبل از تغییر:
```python
def calculate_overall_signal(self):
    # محاسبه امتیاز هر دسته
    trend_score = calc_category_score(self.trend_indicators)
    
    # وزن‌دهی ثابت
    overall_score = (trend_score * 0.30) + (momentum_score * 0.25) + ...
    
    # اعتماد فقط بر اساس توافق
    confidence = 1 - (std_dev / 4)
```

#### بعد از تغییر:
```python
def calculate_overall_signal(self):
    # محاسبه امتیاز و دقت هر دسته
    trend_score, trend_accuracy = calc_category_score_and_accuracy(...)
    
    # تعدیل وزن‌ها بر اساس دقت
    adjusted_weights = calculate_accuracy_adjusted_weights(...)
    
    # وزن‌دهی داینامیک
    overall_score = (trend_score * adjusted_weights['trend']) + ...
    
    # اعتماد = 60% توافق + 40% دقت
    confidence = (agreement * 0.6) + (accuracy * 0.4)
```

**تغییرات کلیدی:**
- ✅ محاسبه میانگین دقت هر دسته
- ✅ تعدیل وزن‌ها بر اساس دقت
- ✅ اعتماد کلی شامل دو عامل: توافق (60%) + دقت (40%)

### 2. ✅ مستندات جدید

#### `ACCURACY_GUIDE.md` (جدید)
راهنمای کامل استفاده از دقت شامل:
- مفهوم دقت و اهمیت آن
- نحوه محاسبه در سه سطح (اندیکاتور، دسته، کلی)
- مثال‌های عملی با سناریوهای مختلف
- توضیح وزن‌دهی داینامیک
- ارتباط با نظریه داو

#### `SIGNAL_CALCULATION.md` (به‌روز‌رسانی)
- ✅ اضافه شدن بخش "تعدیل وزن‌ها بر اساس دقت"
- ✅ مثال‌های محاسباتی با دقت
- ✅ کد به‌روز‌شده با دقت

#### `README.md` (به‌روز‌رسانی)
- ✅ اضافه شدن لینک به `ACCURACY_GUIDE.md`
- ✅ توضیح مختصر درباره تعدیل وزن‌ها

### 3. ✅ تست‌های جامع

#### `test_accuracy_weighting.py`
تست سناریوهای مختلف دقت و تأثیر آن بر اعتماد کلی

#### `test_weight_adjustment.py`
نمایش دقیق تغییرات وزن‌ها در سناریوهای مختلف

#### `test_comprehensive_accuracy.py`
تست‌های جامع شامل:
- همه صعودی با دقت بالا → اعتماد 96%
- سیگنال‌های متناقض → اعتماد 83.8%
- فقط روند دقیق → اعتماد 72.6%
- مومنتوم/سیکل دقیق → اعتماد 76.2%
- همه دقت پایین → اعتماد 72%

## 🔍 چگونه کار می‌کند؟

### قبل (وزن‌های ثابت):
```
Trend: 30% (همیشه)
Momentum: 25% (همیشه)
Cycle: 25% (همیشه)
Volume: 20% (همیشه)
```

### بعد (وزن‌های داینامیک):
```
اگر دقت‌ها: Trend=0.95, Momentum=0.6, Cycle=0.6, Volume=0.6

وزن‌های تعدیل‌شده:
  Trend: 40.4% ⬆️ (+34.8%)
  Momentum: 21.3% ⬇️ (-14.9%)
  Cycle: 21.3% ⬇️ (-14.9%)
  Volume: 17.0% ⬇️ (-15.0%)
```

**نتیجه:** روند با دقت بالاتر، تأثیر بیشتری دارد!

## 📊 مثال عملی

فرض کنید 3 اندیکاتور روند داریم:

**قبل از تغییر:**
```
SMA: Bullish × 0.8 confidence
EMA: Bullish × 0.3 confidence (نامطمئن)
MACD: Bullish × 0.9 confidence

امتیاز = (1×0.8 + 1×0.3 + 1×0.9) / 3 = 0.67
وزن در محاسبه نهایی = 30% (ثابت)
```

**بعد از تغییر:**
```
SMA: Bullish × 0.8 confidence
EMA: Bullish × 0.3 confidence (نامطمئن)
MACD: Bullish × 0.9 confidence

امتیاز = (1×0.8 + 1×0.3 + 1×0.9) / (0.8+0.3+0.9) = 1.0
دقت متوسط = (0.8 + 0.3 + 0.9) / 3 = 0.67

اگر سایر دسته‌ها دقت بالاتر داشته باشند:
→ وزن روند کاهش می‌یابد (مثلاً به 25%)

اگر سایر دسته‌ها دقت پایین‌تر داشته باشند:
→ وزن روند افزایش می‌یابد (مثلاً به 35%)
```

## 💡 مزایا

1. **🎯 دقت بیشتر**: اندیکاتورهای مطمئن‌تر تأثیر بیشتری دارند
2. **🛡️ کاهش خطا**: سیگنال‌های نامطمئن کمتر تأثیرگذارند
3. **📊 شفافیت**: کاربر می‌بیند چرا یک تصمیم گرفته شده
4. **🔄 انعطاف**: وزن‌ها به صورت داینامیک تغییر می‌کنند
5. **✅ سازگار با نظریه داو**: سیگنال‌های قوی‌تر = روند قوی‌تر

## 🧪 نتایج تست‌ها

| سناریو | سیگنال | اعتماد | توضیحات |
|--------|---------|--------|---------|
| همه صعودی + دقت بالا | صعودی | 96% | شرایط ایده‌آل ✅ |
| متناقض + دقت بالا | خنثی | 83.8% | عدم توافق ⚠️ |
| فقط روند دقیق | صعودی شکسته | 72.6% | اتکا به روند |
| مومنتوم/سیکل دقیق | صعودی شکسته | 76.2% | تغییر احتمالی |
| همه دقت پایین | صعودی | 72% | نامطمئن ❌ |

## 📚 فایل‌های تغییر یافته

### کد اصلی:
- ✅ `models/schemas.py` - متد `calculate_overall_signal()`

### مستندات:
- ✅ `ACCURACY_GUIDE.md` (جدید) - راهنمای کامل
- ✅ `SIGNAL_CALCULATION.md` (به‌روز) - فرمول‌ها
- ✅ `README.md` (به‌روز) - لینک‌ها

### تست‌ها:
- ✅ `test_accuracy_weighting.py` (جدید)
- ✅ `test_weight_adjustment.py` (جدید)
- ✅ `test_comprehensive_accuracy.py` (جدید)

## 🚀 استفاده

```python
from models.schemas import TechnicalAnalysisResult, IndicatorResult

# ایجاد اندیکاتورها با confidence
indicators = [
    IndicatorResult(
        indicator_name="SMA",
        signal=SignalStrength.BULLISH,
        confidence=0.9  # دقت بالا
    ),
    IndicatorResult(
        indicator_name="EMA",
        signal=SignalStrength.BULLISH,
        confidence=0.4  # دقت پایین
    )
]

# محاسبه سیگنال کلی
analysis = TechnicalAnalysisResult(...)
analysis.calculate_overall_signal()

# نتایج شامل:
# - overall_signal: سیگنال نهایی
# - overall_confidence: اعتماد (0-1)
#   * تابعی از توافق (60%) و دقت (40%)
```

## ✅ تکمیل شده

همه تغییرات پیاده‌سازی، تست و مستندسازی شده‌اند. سیستم اکنون:
- ✅ دقت را در محاسبه امتیاز هر دسته لحاظ می‌کند
- ✅ وزن‌های نهایی را بر اساس دقت تعدیل می‌کند
- ✅ اعتماد کلی را از توافق و دقت محاسبه می‌کند
- ✅ مستندات کامل دارد
- ✅ تست‌های جامع دارد

**دقت (Accuracy) به طور کامل در امتیازدهی کلی لحاظ شده است! ✨**
