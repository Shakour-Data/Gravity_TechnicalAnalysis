# 📁 Database Directory - پوشه دیتابیس

## ساختار دیتابیس پروژه

### 🗄️ دیتابیس اصلی
```
../data/gravity_tech.db
```
این **تنها دیتابیس واقعی** پروژه است که شامل تمام داده‌ها می‌شود.

### 📄 فایل‌های Schema
این پوشه فقط شامل **دستورات SQL** است، نه دیتابیس‌های واقعی:

- `complete_schema.sql` - دستورات CREATE TABLE برای تمام جداول

## 📊 جداول دیتابیس

| جدول | توضیحات |
|------|---------|
| `tse_reference` | اطلاعات مرجع شرکت‌های TSE (780 شرکت) |
| `historical_scores` | نتایج تحلیل تکنیکال تاریخی (1.6+ میلیون رکورد) |
| `tool_performance_history` | سابقه عملکرد ابزارهای تحلیل |
| `tool_performance_stats` | آمار و خلاصه عملکرد ابزارها |
| `ml_weights_history` | تاریخچه وزن‌های مدل‌های یادگیری ماشین |
| `tool_recommendations_log` | لاگ توصیه‌های ابزارها |
| `market_data_cache` | کش داده‌های قیمتی بازار (OHLCV) |
| `pattern_detection_results` | نتایج شناسایی الگوهای نموداری |

## 🔧 مدیریت دیتابیس

### ایجاد دیتابیس از ابتدا:
```bash
python scripts/setup/complete_setup.py
```

### بارگذاری داده‌های تاریخی:
```bash
python scripts/setup/load_all_historical.py
```

### پر کردن جداول اضافی:
```bash
python scripts/setup/populate_all_tables.py
```

## ⚠️ نکات مهم

1. **فقط یک دیتابیس داریم**: `data/gravity_tech.db`
2. **این پوشه فقط schema دارد** نه دیتابیس واقعی
3. **هیچ دیتابیس اضافی نیاز نیست** - همه چیز در یک فایل است
4. دیتابیس از TSE خارجی داده می‌گیرد: `E:\Shakour\MyProjects\GravityTseHisPrice\data\tse_data.db`

## 📝 Foreign Keys

تمام جداول از طریق فیلد `ticker` به جدول `tse_reference` متصل هستند.
