apiVersion: v1
kind: Secret
metadata:
  name: technical-analysis-secret
  namespace: tech-analysis-prod
type: Opaque
stringData:
  SECRET_KEY: "CHANGE-THIS-IN-PRODUCTION"
  JWT_ALGORITHM: "HS256"
  JWT_EXPIRATION_MINUTES: "30"
---
# Note: In production, use external secret management like HashiCorp Vault or AWS Secrets Manager
# Example with External Secrets Operator:
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: technical-analysis-secret
#   namespace: tech-analysis-prod
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: technical-analysis-secret
#   data:
#     - secretKey: SECRET_KEY
#       remoteRef:
#         key: technical-analysis/secret-key
